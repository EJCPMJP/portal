"use client";import {signIn} from "next-auth/react";import {useState} from "react";import {useRouter} from "next/navigation";export default function Login(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[err,setErr]=useState(null as any);const r=useRouter();async function sub(e:any){e.preventDefault();setErr(null);const res=await signIn("credentials",{redirect:false,email,password});if(res?.ok) r.push("/admin"); else setErr("Credenciais inválidas");}return(<div className="max-w-sm mx-auto token-surface border token-border rounded-2xl p-6"><h1 className="text-xl font-semibold mb-4">Entrar</h1><form onSubmit={sub} className="grid gap-3"><input className="px-3 py-2 rounded-lg token-surface border token-border" placeholder="E-mail" value={email} onChange={e=>setEmail(e.target.value)}/><input className="px-3 py-2 rounded-lg token-surface border token-border" placeholder="Senha" type="password" value={password} onChange={e=>setPassword(e.target.value)}/><button className="px-3 py-2 rounded-lg" style={{background:"var(--accent)"}}>Entrar</button>{err&&<p className="text-sm" style={{color:"tomato"}}>{String(err)}</p>}<p className="text-xs">Use ADMIN_EMAIL/ADMIN_PASSWORD no .env</p></form></div>);}